# Required infrastructure settings (match your Redis instance)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=change_me_strong_password

# App credentials, model choice, and workspace scope are configured in the UI Settings panel.
